<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>503 — Service Unavailable</title>
  <style>
    :root{
      --bg:#0b0b0b;
      --card:#111;
      --accent:#e50914;
      --muted:#bdbdbd;
      --glass: rgba(255,255,255,0.03);
    }
    *{box-sizing:border-box}
    html,body{height:100%;margin:0;font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;color:#fff;background:linear-gradient(180deg,var(--bg) 0%, #070707 100%)}
    .wrap{min-height:100%;display:flex;flex-direction:column;justify-content:center;align-items:center;padding:40px}
    .card{background:var(--card);border-radius:12px;padding:36px;max-width:880px;width:100%;box-shadow:0 10px 30px rgba(0,0,0,0.6);display:grid;grid-template-columns:1fr 320px;gap:24px;align-items:center}
    .left h1{font-size:56px;margin:0 0 10px;color:var(--accent);letter-spacing:1px}
    .left p{color:var(--muted);font-size:18px;margin:0 0 20px;line-height:1.5}
    .actions{display:flex;gap:12px;flex-wrap:wrap}
    .btn{padding:12px 18px;border-radius:8px;border:0;cursor:pointer;font-weight:600}
    .btn.primary{background:var(--accent);color:#fff}
    .btn.ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--muted)}
    .right{display:flex;flex-direction:column;align-items:center;gap:14px}
    .badge{background:var(--glass);padding:8px 12px;border-radius:999px;color:var(--muted);font-size:13px}
    .status{font-size:38px;color:#fff;font-weight:700}
    .desc{color:var(--muted);text-align:center}
    .footer{margin-top:18px;font-size:13px;color:#9a9a9a;text-align:center}
    footer small{opacity:0.7}
    @media (max-width:880px){
      .card{grid-template-columns:1fr; padding:24px}
      .left h1{font-size:38px}
      .status{font-size:32px}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card" role="main" aria-labelledby="title">
      <div class="left">
        <h1 id="title">503 — Service Unavailable</h1>
        <p>Sorry — this content is not available in your region at the moment. Our engineers are likely working on it. Please try again in a few minutes.</p>
        <div class="actions" role="group" aria-label="actions">
          <button class="btn primary" onclick="window.location.reload(true)">Try again</button>
          <button class="btn ghost" onclick="window.location.href='/'">Go to home</button>
        </div>

        <div class="footer">
          <small>If the problem persists, contact support at <strong>support@net-flix.xyz</strong> and quote the reference code below.</small>
        </div>
      </div>

      <div class="right" aria-hidden="false">
        <div class="badge">Reference</div>
        <div class="status">503</div>
        <div class="desc">Service Unavailable — default routing</div>
        <div style="font-size:12px;color:var(--muted);text-align:center">
          Server: <strong>Netflix-Service-Unavailable-Server</strong><br/>
          Private IP: <code>10.1.1.5</code>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
